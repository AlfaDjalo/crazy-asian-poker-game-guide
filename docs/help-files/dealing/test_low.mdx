---
title: Test Low
description: Testing Board Viewer
sidebar_position: 4
---

import PokerBoardViewer from "@site/src/components/PokerBoardViewer";
import PokerHandEvaluator from "@site/src/components/PokerHandEvaluator";

This page tests the PokerBoardViewer.

<PokerBoardViewer
  configPath="/data/boards/single-board.json"
  playerHandSize={4}
  dealPlayerHand={true}
  mode="low"
  renderExtra={({ playerHand, boardState, mode }) => {
    const boardCards = boardState
      ? boardState.flat().filter(cell => cell && cell.card).map(cell => cell.card)
      : [];
    return <PokerHandEvaluator playerHand={playerHand} boardCards={boardCards} mode={mode} />;
  }}
/>
